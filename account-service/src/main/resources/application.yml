server:
  port: 8083

spring:
  profiles:
    active: local
  application:
    name: account-service
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/